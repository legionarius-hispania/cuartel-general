# Archivo histórico

`index_html_snapshot_antiguo.html` es una copia antigua y desincronizada de `app/web/index.html`, encontrada en la raíz del repositorio durante la auditoría del 2026-09-09. Le faltan como mínimo estas mejoras ya entregadas en `app/web/index.html`: el ajuste de ancho a pantalla completa, la corrección de contraste de campos de texto sueltos, la reestructuración del checklist de preparación en 3 partes, y el coste por unidad de camisetas en blanco por color/talla (probablemente también las más recientes: paginación, campos de TikTok/seguimiento y adjuntar etiquetas de envío).

Ningún workflow de GitHub Actions ni `app/main.py` la referencian. Se conserva aquí solo como referencia histórica; **no es la app en uso**. Las versiones activas son `app/web/index.html` (escritorio) y `app/tablet/index.html` (tablet).

Si no la necesitas, puedes borrar esta carpeta entera sin que afecte a la app.
